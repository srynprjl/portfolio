# Sample Post

SAMPLE: Delete this file (and its entry in `blogs.json`) or replace it
with a real post.

Write posts in standard Markdown. Headings, lists, quotes, inline
`code`, fenced code blocks, links, and images all render with the
site theme.
